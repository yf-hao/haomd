
用户画像：
1. 生成摘要

带手写摘要：
在某个文档的 AI Chat 中输入：
/remember 以后所有回答尽量用中文，少废话，多给具体步骤。
预期：
弹窗提示：“已将你手写摘要加入 Global Memory 队列（自动总结失败，未加入）”或“手写 + 自动总结一起加入……”（视自动摘要是否成功）。
后续在其他文档提问时，Global Memory 更新后，模型应更倾向按这个偏好回答。

纯自动摘要：
在一段对话后（有足够来回，至少几条 user/assistant 消息），输入 /remember；
预期弹窗：“已根据当前会话生成摘要并加入 Global Memory 队列。” 或相应失败文案。

2. user Persona -> update now