{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "enables dialog save permission",
  "windows": ["main"],
  "permissions": [
    "dialog:default",
    "dialog:allow-save"
  ]
}
